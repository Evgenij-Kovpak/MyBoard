@model ProductListViewModel

    <h1>Все товары</h1>
    <h2>@Model.productCategory</h2>

    <div class="row mt-5 mb-2">
        @{
            foreach (Product product in Model.allProduct)
            {
                @Html.Partial("AllProducts", product)
            }

        }
    </div>