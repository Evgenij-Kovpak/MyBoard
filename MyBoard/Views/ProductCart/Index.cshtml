@model ProductCartViewModel

<div class="container">
    @foreach (var el in Model.productCart.listCartItem)
    {
        <div class="alert alert-warning mt-3">
            <b>Автомобиль:</b>@el.product.name<br />
            <b>Цена:</b>@el.product.price.ToString("c")

        </div>
    }

    <hr />
    <a class="btn btn-danger" asp-controller="Order" asp-action="CheckOut">Оплатить</a>
</div>
